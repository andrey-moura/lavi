#if __linux__
  #include 'debugger/linux/debugger.andy'
#else
  throw 'Unsupported platform'
#end

if __argv__.size < 3
  out 'andy --debugger <path_to_executable> <args>'

  return 1
end

var debugger = Debugger.new(__argv__[2])

debugger.run