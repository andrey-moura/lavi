type Debugger
  var executable_path = null

  fn init(path)
    executable_path = path

    out 'Initializing debugger for ' + executable_path
  end

  fn run
    out 'Running debugger for ' + executable_path
  end
end
