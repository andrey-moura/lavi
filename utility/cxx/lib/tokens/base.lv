namespace CXX
  namespace Tokens
    // Base class for tokens.
    class Base
      fn match?(source)
        // To be implemented by subclasses.
        return false
      end

      fn extract(source)
        // To be implemented by subclasses.
        return null
      end
    end
  end
end