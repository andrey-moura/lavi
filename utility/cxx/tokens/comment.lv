#include 'base.lv'

namespace CXX
  namespace Tokens
    // Matcher for comment tokens.
    type Comment extends Base
      fn match?(content)
        return content.starts_with?("//") || content.starts_with?("/*")
      end

      fn extract(content)
        
      end
    end
  end
end